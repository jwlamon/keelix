; grafana.ini with no [security] section and no admin creds set.
; Built-in defaults are admin/admin — absent keys should be treated as defaults.
[server]
http_port = 3000

[database]
type = sqlite3
path = grafana.db

[auth]
disable_login_form = false
