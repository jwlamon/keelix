; smb.conf with guest ok written at the top level (no section header)
; This tests that parseSmbConf catches top-level guest ok directives.
guest ok = yes

[global]
   workgroup = WORKGROUP
   server string = Samba Server
   security = user
