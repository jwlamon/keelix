; grafana.ini where admin_user is renamed but admin_password is still "admin".
; R3-2: OR→AND fix — renamed user + default password must still fire SVC020.
[server]
http_port = 3000

[auth.anonymous]
enabled = false

[security]
admin_user = ops
admin_password = admin
