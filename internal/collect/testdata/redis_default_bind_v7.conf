# Redis 7 default bind: 127.0.0.1 plus optional -::1 (IPv6 loopback)
# This config must NOT fire SVC001 — both tokens are loopback.
bind 127.0.0.1 -::1
protected-mode no
# requirepass intentionally absent — protected by loopback bind
