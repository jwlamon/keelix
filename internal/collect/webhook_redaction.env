# Parser-fed fixture for FIX-8 redaction test.
# WEBHOOK_URL holds a high-entropy webhook URL; the entropy gate (not the
# key-name gate) must classify it as "secret" because "WEBHOOK_URL" does not
# match any secretNameToken, but the URL path carries sufficient entropy.
WEBHOOK_URL=https://hooks.example.com/services/xK9mP2nQ7rT4vW8yZ1cB6dF3gH0jL5oMnPqRsTuVwX
# Plain values that must survive verbatim.
LOG_LEVEL=info
PORT=8080
