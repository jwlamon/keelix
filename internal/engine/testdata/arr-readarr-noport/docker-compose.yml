# Readarr with no <Port> in config.xml; port 8787 published to all interfaces.
# Used by TestEngineIntegration_R3_4_PerImagePortFallback.
version: "3.8"
services:
  readarr:
    image: linuxserver/readarr:latest
    ports:
      - "8787:8787"
    volumes:
      - ./config.xml:/config/config.xml:ro
