# Sonarr with no <Port> in config.xml; port 8989 published to all interfaces.
# Used by TestEngineIntegration_R3_4_PerImagePortFallback.
version: "3.8"
services:
  sonarr:
    image: linuxserver/sonarr:latest
    ports:
      - "8989:8989"
    volumes:
      - ./config.xml:/config/config.xml:ro
